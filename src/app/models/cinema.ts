import { Ville } from "./ville";

export class Cinema {
    id?: number;
    name?: string;
    longitude?: string;
    latitude?: string;
    altitude?: string;
    nombreSalles?: number;
    ville?: Ville
}